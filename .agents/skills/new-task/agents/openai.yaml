interface:
  display_name: "Create Planned Task"
  short_description: "Scaffold a planned task contract safely"
  default_prompt: "Use $new-task to scaffold a planned task without activating it or pinning its baseline."
policy:
  allow_implicit_invocation: false
